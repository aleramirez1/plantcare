import '../../../../../core/config.dart';

class AuthRemoteConstants {
  static const String loginEndpoint = '${AppConfig.apiBaseUrl}/login';
  static const String registerEndpoint = '${AppConfig.apiBaseUrl}/register';
}
